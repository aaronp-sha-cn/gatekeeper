# ============================================================
# GateKeeper - Debian Preseed 自动化配置
# 用于Debian 10 (Buster) 自动化安装
# ============================================================

# 跳过 CD-ROM 检测，支持 USB/无光盘安装
d-i debian-installer/cdrom-detect/try-usb boolean true
d-i debian-installer/cdrom-detect/cdrom-detect boolean false
d-i cdrom-detect/load_media boolean false
d-i cdrom-detect/cdrom_device string /dev/sda
d-i cdrom-detect/cdrom_fs_type string iso9660

# 语言和区域设置
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# 网络配置 - 自动选择第一个可用的网卡，允许无网络继续安装
d-i netcfg/choose_interface select auto
d-i netcfg/link_wait_timeout string 10
d-i netcfg/dhcp_timeout string 30
d-i netcfg/dhcp_failed note
d-i netcfg/dhcp_options select Do not configure the network at this time
d-i netcfg/disable_autoconfig boolean true
d-i netcfg/get_hostname string gatekeeper
d-i netcfg/get_domain string local

# 镜像源 - Debian 10 已归档，必须使用 archive.debian.org
# 启用 non-free 以包含网卡固件
d-i mirror/country string manual
d-i mirror/http/hostname string archive.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string
d-i mirror/suite string buster
d-i mirror/udeb/suite string buster

# 包含非自由固件（网卡驱动）
d-i hw-detect/load_firmware boolean true

# 时区
d-i time/zone string Asia/Shanghai
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean true

# 分区配置 - 完全自动分区（使用整个磁盘，无任何交互）
d-i partman-auto/method string regular
d-i partman-auto/disk string /dev/sda
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman-partitioning/confirm_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman-md/confirm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true

# 用户配置
d-i passwd/root-login boolean true
d-i passwd/root-password-crypted password $6$Kje5KDszAqndHpxg$T4Tye2dN5ZTZmvxSBMKUd9HO3W1QEBg6hRl4cVHZQU8g9enW1Zn0H3FgjgNjYHLGBshzxyZvvVELRYBDMW/pZ1
d-i passwd/root-password-again password $6$Kje5KDszAqndHpxg$T4Tye2dN5ZTZmvxSBMKUd9HO3W1QEBg6hRl4cVHZQU8g9enW1Zn0H3FgjgNjYHLGBshzxyZvvVELRYBDMW/pZ1

d-i passwd/user-fullname string GateKeeper Admin
d-i passwd/username string gkadmin
d-i passwd/user-password-crypted password $6$4E1ray97FgnjgE5t$wLcVGG0ZUgF0lmGpBjnb.Lm1j6UsRYlp5bGB8T31uWuzA9CgLyeAW3ZcznrG/A8rvl.Qug3/HXdQ3v7TwNV8I0
d-i passwd/user-password-again password $6$4E1ray97FgnjgE5t$wLcVGG0ZUgF0lmGpBjnb.Lm1j6UsRYlp5bGB8T31uWuzA9CgLyeAW3ZcznrG/A8rvl.Qug3/HXdQ3v7TwNV8I0

# 跳过用户创建交互（使用预配置值，不弹出对话框）
d-i passwd/user-uid string
d-i passwd/user-default-groups string sudo,adm,cdrom,dip,plugdev,netdev

# 软件包选择 - 极简安装，仅 SSH 服务器
# 注意：不选 standard 任务（会安装数百个不必要的包，从 archive.debian.org 下载极慢）
# 所有其他依赖由 first-start.sh 在启动时安装
d-i pkgsel/early_package string ssh
tasksel tasksel/first multiselect ssh-server
tasksel tasksel/skip-tasks multiselect standard
d-i pkgsel/include string \
    python3 \
    python3-pip \
    python3-venv \
    git \
    curl \
    wget \
    net-tools \
    iproute2 \
    iptables \
    sudo

d-i pkgsel/upgrade select none
d-i pkgsel/update-policy select none

# 引导加载器
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string default

# 配置GRUB使用传统网卡命名 (eth0, eth1, ...)
d-i debian-installer/add-kernel-opts string net.ifnames=0 biosdevname=0

# 跳过额外 CD/DVD 扫描
d-i apt-setup/cdrom/set-first boolean false
d-i apt-setup/use_cdrom boolean false

# 禁用安全源探测 - Debian 10 (Buster) 已 EOL，security.debian.org 不可用
d-i apt-setup/security_host string
d-i apt-setup/services-select multiselect

# 完成安装
d-i finish-install/reboot_in_progress note
# 禁止弹出CD，确保late_command可以访问/cdrom/
d-i cdrom-detect/eject boolean false

# Early command - 检测并挂载USB设备到/cdrom
d-i preseed/early_command string \
    echo "Starting GateKeeper installation..."; \
    mkdir -p /cdrom; \
    for dev in /dev/sdb /dev/sdc /dev/sdd /dev/sde; do \
        mount -o ro $dev /cdrom 2>/dev/null && break; \
    done; \
    if [ ! -f /cdrom/preseed.cfg ]; then \
        for dev in /dev/sdb1 /dev/sdc1 /dev/sdd1 /dev/sde1; do \
            mount -o ro $dev /cdrom 2>/dev/null && break; \
        done; \
    fi; \
    echo "/cdrom" > /var/lib/cdrom-identification 2>/dev/null || true

# 禁用软驱检测
d-i hw-detect/floppy_detected boolean false

# Late command - 执行安装后脚本（从已挂载的/cdrom）
d-i preseed/late_command string \
    if [ -f /cdrom/late-command.sh ]; then \
        cp /cdrom/late-command.sh /target/tmp/late-command.sh && \
        chmod +x /target/tmp/late-command.sh && \
        sh /target/tmp/late-command.sh && \
        rm -f /target/tmp/late-command.sh; \
    fi

# Popularity contest - 不参与
popularity-contest popularity-contest/participate boolean false
