# ============================================================
# GateKeeper - Debian Preseed 自动化配置
# 用于 Debian 10 (Buster) 通过 USB (isohybrid) 自动化安装
# ============================================================
#
# 关键修复说明：
# - ISO 通过 isohybrid 处理后，USB 被内核识别为 /dev/sr0（光驱设备）
# - 恢复网络镜像源，确保 apt 有后备源，避免 grub-pc CD-ROM 读取失败
# - early_command / late_command 使用 /dev/sr0 挂载
# - 所有交互提示均设为自动确认，实现完全无人值守安装
# ============================================================

# ---- 语言和区域设置 ----
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# ---- 网络配置 ----
# 自动选择网络接口，DHCP 获取 IP
# 如果 DHCP 失败，使用静态 IP 作为后备（确保网络可用）
d-i netcfg/choose_interface select auto
d-i netcfg/link_wait_timeout string 10
d-i netcfg/dhcp_timeout string 30
d-i netcfg/dhcp_failed note
# DHCP 失败时使用静态 IP（后备方案，确保网络可用）
d-i netcfg/dhcp_options select Configure network manually
d-i netcfg/get_ipaddress string 192.168.1.100
d-i netcfg/get_netmask string 255.255.255.0
d-i netcfg/get_gateway string 192.168.1.1
d-i netcfg/get_nameservers string 8.8.8.8
d-i netcfg/confirm_static boolean true
d-i netcfg/get_hostname string gatekeeper
d-i netcfg/get_domain string local

# ---- 镜像源配置 ----
# 恢复网络镜像源（关键修复：之前为空导致 apt 无后备源，grub-pc 安装失败）
d-i mirror/country string manual
d-i mirror/http/hostname string archive.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# 禁用 CD-ROM 作为 apt 源（防止 grub-pc 尝试从 CD-ROM 读取文件）
d-i apt-setup/cdrom/set-first boolean false
d-i apt-setup/use_cdrom boolean false
d-i apt-setup/cdrom/set-double boolean false
d-i apt-setup/cdrom/set-next boolean false

# 禁用安全更新源（Debian 10 已 EOL，安全更新已合并到主归档）
d-i apt-setup/security_host string
d-i apt-setup/services-select multiselect

# ---- 固件 ----
d-i hw-detect/load_firmware boolean true
d-i hw-detect/floppy_detected boolean false

# ---- 时区 ----
d-i time/zone string Asia/Shanghai
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean false

# ---- 分区配置 ----
# 自动使用整个 /dev/sda 磁盘，所有确认项均为 true（完全无人值守）
d-i partman-auto/method string regular
d-i partman-auto/disk string /dev/sda
d-i partman-auto/choose_recipe select atomic
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman/confirm_write_new_label boolean true
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman-partitioning/confirm_new_label boolean true
d-i partman-md/confirm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true

# ---- 用户配置 ----
d-i passwd/root-login boolean true
d-i passwd/root-password-crypted password \$6\$Kje5KDszAqndHpxg\$T4Tye2dN5ZTZmvxSBMKUd9HO3W1QEBg6hRl4cVHZQU8g9enW1Zn0H3FgjgNjYHLGBshzxyZvvVELRYBDMW/pZ1
d-i passwd/user-fullname string GateKeeper Admin
d-i passwd/username string gkadmin
d-i passwd/user-password-crypted password \$6\$4E1ray97FgnjgE5t\$wLcVGG0ZUgF0lmGpBjnb.Lm1j6UsRYlp5bGB8T31uWuzA9CgLyeAW3ZcznrG/A8rvl.Qug3/HXdQ3v7TwNV8I0
d-i passwd/user-default-groups string sudo,adm,cdrom,dip,plugdev,netdev

# ---- 软件包选择 ----
# 不安装额外软件包，所有依赖由 first-start.sh 在首次启动后通过网络安装
tasksel tasksel/first multiselect
tasksel tasksel/skip-tasks multiselect standard, desktop
d-i pkgsel/include string
d-i pkgsel/upgrade select none
d-i pkgsel/update-policy select none

# ---- 引导加载器配置 ----
# 使用 default 让安装器自动选择引导设备（不硬编码 /dev/sda）
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string default
d-i grub-installer/skip boolean false

# grub-pc 预配置：指定安装到 /dev/sda，避免弹出交互界面
grub-pc grub-pc/install_devices_empty boolean false
grub-pc grub-pc/install_devices string /dev/sda
grub-pc grub-pc/timeout string 3
grub-pc grub-pc/make_install_device_bootable boolean true

# 内核参数：使用传统网卡命名（eth0 而非 enp3s0）
d-i debian-installer/add-kernel-opts string net.ifnames=0 biosdevname=0

# ---- 完成安装 ----
d-i finish-install/reboot_in_progress note
d-i cdrom-detect/eject boolean false

# ---- Popularity contest ----
popularity-contest popularity-contest/participate boolean false

# ---- Early command ----
# isohybrid 处理后 USB 被内核识别为 /dev/sr0（光驱设备）
# 确保安装器能找到 preseed.cfg 和后续的 late-command.sh
d-i preseed/early_command string \
    mkdir -p /cdrom; \
    if mount -o ro /dev/sr0 /cdrom 2>/dev/null; then \
        echo "[early_command] Mounted /dev/sr0 to /cdrom"; \
    else \
        echo "[early_command] WARNING: Failed to mount /dev/sr0, trying other devices..."; \
        for dev in /dev/sdb /dev/sdc /dev/sdd /dev/sde; do \
            mount -o ro $dev /cdrom 2>/dev/null && echo "[early_command] Mounted $dev" && break; \
        done; \
    fi; \
    echo "/cdrom" > /var/lib/cdrom-identification 2>/dev/null || true

# ---- Late command ----
# 重新挂载 /dev/sr0 并执行 late-command.sh
d-i preseed/late_command string \
    mkdir -p /cdrom; \
    if mount -o ro /dev/sr0 /cdrom 2>/dev/null; then \
        echo "[late_command] Mounted /dev/sr0"; \
    else \
        echo "[late_command] WARNING: Failed to mount /dev/sr0, trying other devices..."; \
        for dev in /dev/sdb /dev/sdc /dev/sdd /dev/sde; do \
            mount -o ro $dev /cdrom 2>/dev/null && echo "[late_command] Mounted $dev" && break; \
        done; \
    fi; \
    if [ -f /cdrom/late-command.sh ]; then \
        cp /cdrom/late-command.sh /target/tmp/late-command.sh && \
        chmod +x /target/tmp/late-command.sh && \
        sh /target/tmp/late-command.sh; \
    else \
        echo "ERROR: late-command.sh not found on /cdrom"; \
    fi
